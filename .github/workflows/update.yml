name: Weekly ReelRadar Update

on:
  schedule:
    # Every Friday at 9:00 AM IST (3:30 AM UTC) — fresh for the weekend
    - cron: "30 3 * * 5"
  workflow_dispatch: {}   # also lets you run it manually anytime with one click

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout site
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Fetch latest releases
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
        run: node scripts/update.js

      - name: Commit updated data
        run: |
          git config user.name "reelradar-bot"
          git config user.email "actions@github.com"
          git add data.json
          git diff --staged --quiet || git commit -m "Weekly release scan: $(date -u +%Y-%m-%d)"
          git push
